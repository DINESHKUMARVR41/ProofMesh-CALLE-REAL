import {NextResponse} from 'next/server'
import {extractClaims,extractText,inferKind,persistUpload,sha256} from '@/lib/proofmesh/extract'
import {makeCase} from '@/lib/proofmesh/engine'
import {geminiClaims} from '@/lib/proofmesh/gemini'
import {saveCase} from '@/lib/proofmesh/store'
import type {SourceDocument} from '@/lib/proofmesh/types'
export const runtime='nodejs'
export async function POST(req:Request){try{const form=await req.formData();const files=form.getAll('files').filter(x=>x instanceof File) as File[];if(!files.length)return NextResponse.json({error:'Upload at least one document.'},{status:400});if(files.length>12)return NextResponse.json({error:'Maximum 12 documents per case.'},{status:400});const docs:SourceDocument[]=[];const allClaims=[] as ReturnType<typeof extractClaims>;for(const file of files){const bytes=Buffer.from(await file.arrayBuffer());if(bytes.length>15*1024*1024)throw new Error(`${file.name} is larger than 15 MB.`);const id=crypto.randomUUID();const hash=await sha256(bytes);let text=''; let pageCount:number|undefined; try { const r=await extractText(file.name,file.type,bytes); text=r.text; pageCount=r.pageCount } catch (e) { if (!process.env.GEMINI_API_KEY || !file.type.startsWith('image/')) throw e; const b64=bytes.toString('base64'); const rr=await fetch(`https://generativelanguage.googleapis.com/v1beta/models/${process.env.GEMINI_MODEL||'gemini-2.5-flash'}:generateContent?key=${encodeURIComponent(process.env.GEMINI_API_KEY)}`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({contents:[{parts:[{inlineData:{mimeType:file.type,data:b64}},{text:'Transcribe this business document faithfully. Preserve quantities, dates, names, identifiers, prices and totals. Return plain text only.'}]}],generationConfig:{temperature:0}})}); if(!rr.ok)throw new Error('Gemini image extraction failed: '+await rr.text()); const jj=await rr.json(); text=jj.candidates?.[0]?.content?.parts?.map((p:any)=>p.text||'').join('')||'' }const doc:SourceDocument={id,name:file.name,mimeType:file.type,size:bytes.length,sha256:hash,kind:inferKind(file.name),createdAt:new Date().toISOString(),textPreview:text.replace(/\s+/g,' ').trim().slice(0,900),pageCount};docs.push(doc);const ai=await geminiClaims(doc,text).catch(()=>[]); allClaims.push(...(ai.length?ai:extractClaims(doc,text)));await persistUpload(id,file.name,bytes)}const c=makeCase(String(form.get('caseName')||`Evidence case ${new Date().toLocaleString()}`),docs,allClaims);await saveCase(c);return NextResponse.json(c)}catch(e){return NextResponse.json({error:e instanceof Error?e.message:'Analysis failed'},{status:500})}}
