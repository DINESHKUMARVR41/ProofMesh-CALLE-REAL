import {NextResponse} from 'next/server'; import crypto from 'node:crypto'; import {getCase,saveCase} from '@/lib/proofmesh/store'; import type {Investigation} from '@/lib/proofmesh/types'
export const runtime='nodejs'
const schema={type:'object',additionalProperties:false,required:['supplier_claim','requested_evidence','confidence'],properties:{supplier_claim:{type:'string',description:'The contact explanation as a claim, not verified evidence.'},requested_evidence:{type:'array',items:{type:'string'}},confidence:{type:'string'}}}
export async function POST(req:Request,{params}:{params:Promise<{id:string}>}){const{id}=await params;const c=await getCase(id);if(!c)return NextResponse.json({error:'Case not found'},{status:404});const body=await req.json();const finding=c.findings.find(f=>f.id===body.findingId);if(!finding)return NextResponse.json({error:'Finding not found'},{status:404});const phone=String(body.phone||'').trim();if(!phone)return NextResponse.json({error:'Phone number is required.'},{status:400});const investigation:Investigation={id:crypto.randomUUID(),findingId:finding.id,phone,contactName:String(body.contactName||'supplier contact'),status:'draft',goal:`Investigate ProofMesh finding: ${finding.title}. Explain the discrepancy using only facts available to you. If you claim that additional goods/documents exist, identify the document or record that can prove it. Do not claim the issue is verified. Missing evidence: ${finding.missingEvidence.join('; ')}`,createdAt:new Date().toISOString()};
c.investigations.push(investigation);finding.status='investigating';c.status='investigating';c.summary.unresolved=c.findings.filter(f=>f.status==='unresolved'||f.status==='investigating').length;await saveCase(c)
if(process.env.CALLE_API_KEY){try{const res=await fetch('https://api.heycall-e.com/v1/calls',{method:'POST',headers:{Authorization:`Bearer ${process.env.CALLE_API_KEY}`,'Content-Type':'application/json','Idempotency-Key':`proofmesh-${investigation.id}`},body:JSON.stringify({task:investigation.goal,recipients:[{phones:[phone],region:String(body.region||'IN'),locale:String(body.locale||'en-IN')}],result_schema:schema,metadata:{proofmesh_case_id:c.id,finding_id:finding.id,investigation_id:investigation.id}})});if(!res.ok)throw new Error(await res.text());const task=await res.json();investigation.status=task.status==='completed'?'completed':'queued';investigation.callId=task.id;await saveCase(c);return NextResponse.json({case:c,investigation})}catch(e){investigation.status='failed';await saveCase(c);return NextResponse.json({error:`CALL-E request failed: ${e instanceof Error?e.message:'unknown error'}`,case:c},{status:502})}}
return NextResponse.json({case:c,investigation,message:'CALL-E is not configured. Set CALLE_API_KEY to enable real calls.'})}
