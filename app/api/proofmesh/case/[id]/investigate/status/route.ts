import {NextResponse} from 'next/server'; import {getCase,saveCase} from '@/lib/proofmesh/store'
export const runtime='nodejs'
export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){const{id}=await params;const c=await getCase(id);if(!c)return NextResponse.json({error:'Case not found'},{status:404});const inv=c.investigations.at(-1);if(!inv?.callId||!process.env.CALLE_API_KEY)return NextResponse.json({case:c,investigation:inv});const res=await fetch(`https://api.heycall-e.com/v1/calls/${inv.callId}`,{headers:{Authorization:`Bearer ${process.env.CALLE_API_KEY}`}});if(!res.ok)return NextResponse.json({error:await res.text()},{status:502});const task=await res.json();inv.status=task.status==='completed'?'completed':task.status==='in_progress'?'in_progress':task.status==='failed'?'failed':'queued';if(task.structured_result){inv.supplierClaim=task.structured_result.supplier_claim||task.summary||'';inv.requestedEvidence=task.structured_result.requested_evidence||[]};const attempt=task.recipients?.[0]?.attempts?.at(-1);if(attempt?.transcript_turns)inv.transcript=attempt.transcript_turns.map((x:any)=>({role:x.speaker==='bot'?'agent':'human',text:x.text,timestampSeconds:x.offset_seconds}));await saveCase(c);return NextResponse.json({case:c,investigation:inv})}
